# Biscuit for Neovim
Warm and cozy colorscheme for Neovim.

# Usage

> With packer.nvim

```lua
use {
    'Biscuit-Theme/nvim',
    as = "biscuit",
    config = function()
        vim.cmd('colorscheme biscuit')
    end
}
```
